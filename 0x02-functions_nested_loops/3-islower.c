#include "main.h"

/**
 * _islower - checks whether a character c is lower or not using loop
 *
 * Return: 1 is c is lower else 0
 */
int _islower(int)
{
	int c;

	for (c = 0; c < 3; c++)
	{
		if (c islower())
			_putchar(0 + '0')
